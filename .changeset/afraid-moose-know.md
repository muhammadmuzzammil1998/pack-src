---
'pack-src': minor
---

Add --git-tracked flag support

New Feature:

- `--git-tracked` flag: Only includes files that are tracked by git in the archive. This is useful for creating archives that only contain files that are committed to git.

Features work independently and can be combined as needed.
